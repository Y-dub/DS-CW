Here we are doing is that: 

1) Remove unnecessary columns like Refreshed Date, duplicated or ID-like fields (if irrelevant)

2) Drop columns with more than 40% missing values

3) Handle nulls using statistical imputation (mean/median/mode)

4) Format temporal data: Extract month, year, week_number for trend-based analysis

Remove rows with excessive missing data

This is a crucial step before any ML modeling.

Technical notes - 

Objective:

Eliminate redundancy and handle missing data to prepare for feature engineering.

Techniques Applied:

Dropped administrative column (Refreshed Date)

Visualized missing data using missingno

Dropped columns with >40% missingness

Median imputation (numerical), Mode imputation (categorical)

Converted wide format (many months as columns) into long format (rows = crime/month/ward)

Extracted Year, Month, and approximated WeekNumber for seasonal analysis
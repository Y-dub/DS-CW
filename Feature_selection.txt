1)Here we used Univariate FIltering(SelectKBest + f_regreesion) to identify top features. 

2) Then Applied Model-Based feature importance
(Random Forest / XGBoost) to rank features

3) Remove multicollinear features using VIF
(variance Inflation Factor) analysis. 

4) Finalize a clean set of features Based on the interection of statistical and model-based importance

Feature Selection is critical to avoid overfitting and speed up training


Objective : 
Reduce dimensionality,remove redundant or irrelevant features, and retain the most informative ones for predictive modeling. 

Techniques Applied: 
1) SelectKBest : Evaluates each feature's correlation with the target(f_regreesion). - Evaluates each feature's individual correlation with the target(CrimeCount) using F-Statistics.


2)RandomForestRegreesor: Estimates non-linear
feature importance robustly.  - Captures nonlinear dependencies and ranks features using ensemble-based importance


3)VIF : Identifies multicollinear features that can destabilize linear models.  - Measures how much a features is linerly dependant on others. Helps in reducing redundant variables. 

4) Final Step : We took the intersection of the top 15 features from the both Univariate and model based methods. 


Final Output: 
A concise dataset with selected features and the target variable CrimeCount. 



